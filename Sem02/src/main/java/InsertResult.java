public class InsertResult {
    public TreeNode newNode;
    public boolean inserted;

    public InsertResult(TreeNode newNode, boolean inserted) {
        this.newNode = newNode;
        this.inserted = inserted;
    }
}